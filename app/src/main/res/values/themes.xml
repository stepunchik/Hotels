<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Hotels" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">#322d26</item>
    </style>

    <style name="Theme.Hotels" parent="Base.Theme.Hotels" />

    <style name="Theme.Hotels.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Hotels.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Hotels.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    
    <style name="ThemeMaterialCalendar" parent="ThemeOverlay.MaterialComponents.MaterialCalendar">
        <item name="android:windowFullscreen">false</item>
        <item name="colorPrimary">@color/black</item>
    </style>

</resources>